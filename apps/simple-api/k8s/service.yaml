apiVersion: v1
kind: Service
metadata:
  name: simple-api
  namespace: simple-api
  labels:
    app: simple-api
spec:
  selector:
    app: simple-api
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
